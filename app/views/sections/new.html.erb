<h2>Add a New Section in <%= @subject.title %> </h2>

<%= form_for subject_sections_path do |f| %>
    <%#= f.hidden_field :subject_id %>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.submit %>
<% end %>