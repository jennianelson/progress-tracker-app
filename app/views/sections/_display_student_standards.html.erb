
    <table id= "student_standards">
        <tr>
            <th>Number</th>
            <th>Standard</th>
            <th>Progress Level</th>
        </tr>
    <% student_standards.each do |ss| %>
            <tr>
                <td><%= student_standard_dot_notation(ss) %></td>
                <td><%= student_standard_description(ss) %> </td>
                <td><%= progress_column_options(ss) %> <%= link_to "View", student_standard_path(ss) if ss.progress_level  %></td>
            </tr>
    <% end %>
    </table>