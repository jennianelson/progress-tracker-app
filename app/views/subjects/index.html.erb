 <h1>Your Subjects</h1>
    <%= button_to "Add New Subject", new_student_subject_path, method: 'get' %>
    <br>
    <div>
        <% @subjects.each do |s| %>
            <%= link_to s.title, subject_path(s) %>
            <%= button_to "Delete  #{s.title}", student_subject_path(s), method: :delete %> <br>
        <% end %>
    <div>