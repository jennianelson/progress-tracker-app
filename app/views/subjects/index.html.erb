<%# if current_user %>
    <h1>Your Subjects</h1>
    <%= button_to "Add New Subject", new_student_subject_path, method: 'get' %>
    <div>
        <% user_subjects.each do |s| %>
            <p><%= link_to s.title, subject_path(s) %> <p>
        <% end %>
    <div>
<%# end %>

