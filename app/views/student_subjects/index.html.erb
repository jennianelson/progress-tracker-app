 <h1>Your Subjects</h1>
    <%= button_to "Add New Subject", new_student_subject_path, method: 'get' %>
    <br>
    <div>
        <% @student_subjects.each do |s| %>
            <%= link_to subject_title(s), student_subject_path(s) %>
            <%= button_to "Delete  #{subject_title(s)}", student_subject_path(s), method: :delete %> <br>
        <% end %>
    <div>