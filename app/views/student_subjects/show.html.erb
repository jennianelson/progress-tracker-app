<h2><%= student_subject_title(@student_subject) %></h2>
<i><%= link_to "Back to All Subjects", student_subjects_path %></i>
<ul>
<% student_subject_sections(@student_subject).each do |section| %>
    <li><%= link_to section.title, section_path(section) %> </li> <br>
<% end %>
</ul>