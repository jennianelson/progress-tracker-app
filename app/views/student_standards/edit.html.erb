<h2>Update Your Progress</h2>
<h3>
    Section: <i><%= section(@student_standard).title %></i>
    <br> <br>
    Standard: <i><%= description(@student_standard) %></i>
</h3>

<%= form_for [@section, @student_standard] do |f| %>

    <h4>Which of the following best describes your current progress with this skill or concept?</h4>
        <%= f.select :progress_level, StudentStandard.progress_levels.keys %>
    <h4>Comments (For example, how would you explain this skill or concept?):</h4>
        <%= f.text_area :comments %>
    <h4>What projects, labs, tests, class activities, etc. relate to this standard?</h4>
        <%= f.text_area :evidence %> <br>
    <%= f.submit "Save Progress" %>
<% end %>